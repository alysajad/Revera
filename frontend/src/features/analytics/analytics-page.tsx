import { officers } from "@/lib/crm";

export function AnalyticsPage({ personal = false }: { personal?: boolean }) {
  const rows = personal ? officers.slice(0, 1) : officers;
  return <section className="page"><div className="page-heading"><div><p className="eyebrow">{personal ? "MY RESULTS" : "PIPELINE INTELLIGENCE"}</p><h1>{personal ? <>Your work, <span>in focus.</span></> : <>Turn every enquiry into a <span>clear next step.</span></>}</h1></div><div className="period"><button className="selected">This month</button><button>Last month</button><button>▣</button></div></div><section className="analytics-grid"><article className="panel table-panel"><header className="panel-heading"><div><p className="eyebrow">{personal ? "MY PERFORMANCE" : "TEAM PERFORMANCE"}</p><h2>{personal ? "Your conversion rate" : "Who is converting"}</h2></div><button className="clear">Export CSV ↓</button></header><div className="table-scroll"><table><thead><tr><th>Officer</th><th>Assigned</th><th>Called</th><th>Qualified</th><th>Won</th><th>Rate</th></tr></thead><tbody>{rows.map(officer => <tr key={officer.id}><td><b>{officer.name}</b></td><td>{officer.assigned}</td><td>{officer.calls}</td><td>{officer.qualified}</td><td>{officer.won}</td><td><span className="status qualified">{((officer.won / officer.assigned) * 100).toFixed(1)}%</span></td></tr>)}</tbody></table></div></article><article className="panel"><p className="eyebrow">FOLLOW-UP LOAD</p><h2>Next 48 hours</h2><div className="follow-total"><b>{personal ? 4 : 17}</b><span>callbacks due<br /><small>{personal ? "1 overdue" : "5 overdue"}</small></span></div><div className="follow-line"><span><i className="urgent" />Today</span><b>{personal ? 4 : 9} calls</b></div><div className="follow-line"><span><i />Tomorrow</span><b>{personal ? 2 : 8} calls</b></div></article></section></section>;
}
